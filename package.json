{
  "name": "innovateats-outreach-os",
  "version": "0.1.0",
  "private": true,
  "description": "Internal research, outreach, compliance, and sales handoff operating system for InnovatEats.",
  "license": "UNLICENSED",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=11 <12"
  },
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev --parallel",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:integration": "turbo run test:integration",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "db:generate": "pnpm --filter @innovateats/db generate",
    "db:migrate": "pnpm --filter @innovateats/db migrate",
    "db:migrate:check": "pnpm --filter @innovateats/db migrate:check",
    "db:seed": "pnpm --filter @innovateats/db seed",
    "db:studio": "pnpm --filter @innovateats/db studio",
    "deploy:preflight": "pnpm --filter @innovateats/config deploy:preflight",
    "local:prepare": "node scripts/prepare-local-deployment.mjs",
    "local:config": "docker compose --env-file deploy/local.env -f deploy/compose.local.yaml config --quiet",
    "local:build": "docker compose --env-file deploy/local.env -f deploy/compose.local.yaml build",
    "local:up": "docker compose --env-file deploy/local.env -f deploy/compose.local.yaml up -d",
    "local:status": "docker compose --env-file deploy/local.env -f deploy/compose.local.yaml ps",
    "local:down": "docker compose --env-file deploy/local.env -f deploy/compose.local.yaml down"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.1.1",
    "eslint": "10.7.0",
    "prettier": "3.9.5",
    "turbo": "2.10.5",
    "typescript": "5.9.3",
    "typescript-eslint": "8.64.0",
    "vitest": "4.1.10"
  }
}
